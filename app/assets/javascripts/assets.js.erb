<% environment.context_class.instance_eval { include EmberHelper } %>

window.assets = window.assets || {};

window.assets.galleria = <%= raw(galleria_assets.to_json) %>;
window.assets.images = <%= raw(image_assets.to_json) %>;
