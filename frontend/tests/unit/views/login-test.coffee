`import { moduleFor, test } from 'ember-qunit'`

moduleFor 'view:login', 'Unit | View | login'

# Replace this with your real tests.
test 'it exists', (assert) ->
  view = @subject()
  assert.ok view
