`import { moduleFor, test } from 'ember-qunit'`

moduleFor 'view:picture/edit', 'Unit | View | picture/edit'

# Replace this with your real tests.
test 'it exists', (assert) ->
  view = @subject()
  assert.ok view
