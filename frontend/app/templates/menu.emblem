#menu class="modal"
  ul#menu_expanded 
    li ABOUT
    li CONTACT
    li IMPRESSUM
    li
      if session.isAuthenticated
        = link-to 'invalidateSession' | LOGOUT
      else
        = link-to 'login' | LOGIN
